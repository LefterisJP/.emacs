(require 'sr-speedbar)

;; Always have speedbar appear on the right side
(setq sr-speedbar-right-side t)
;; Automatically refresh speedbar
(setq sr-speedbar-auto-refresh t)
;; Skip speedbar window with other-window
(setq sr-speedbar-skip-other-window-p t);

