;; tell geiser that we want racket as the scheme flavour of choice
(setq geiser-active-implementations '(racket))
;; reminder: the command to choose racket module loaded by the repl is:
;; M-x switch-to-geiser-module
